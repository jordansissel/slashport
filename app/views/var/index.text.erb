<% 
data = []
@variables.each do |var| 
  keys = ["component", "section"]
  keys += var.keys.select { |k| !keys.include?(k) }.sort
  data << keys.collect { |k| "#{k}=#{var[k]}" }.join(",")
end
-%>
<%= data.sort.join("\n") %>

